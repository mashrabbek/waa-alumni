spring:
  application:
    name: spring-security-demo-1
  datasource:
    password: Prsard1997
    url: jdbc:mysql://localhost:3306/spring-security-demo-1
    username: root
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
server:
  port: 8080
